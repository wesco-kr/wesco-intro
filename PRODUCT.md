# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 1차: WESCO 영업담당(사업개발본부)이 고객·대리점 미팅에서 발표 모드(`p` 키 / `?present=1`)로 한 쪽씩 넘기며 설명한다. 노트북·회의실 화면.
- 2차: 미팅 뒤 고객(반도체·디스플레이·자동차·이차전지·PCB 등 제조업의 설비·전기·구매 담당)과 해외 대리점이 메일·카카오톡 링크로 다시 연다. 모바일 열람이 잦다. (주인님 확인 260924: 둘 다)
- 언어: 한·영·중·일·베 5개. 언어 버튼 / `?lang=` / 브라우저 자동 감지.

## Product Purpose

순간정전보상장치 TSP® 전문기업 WESCO의 온라인 사업소개서. 14쪽 고정 판형(1120×720) «책» 형식의 디지털 브로셔로, 종이 사업소개서(WESCO-사업소개서-2025-Ver3.5)를 대체한다.
성공 = ①고객이 내부 보고에 쓸 수 있을 만큼 실적·사양·원리를 이해한다(신뢰) ②문의하기·메일·사양서 요청으로 이어진다. (주인님 확인 260924: 둘 다)

## Positioning

배터리 없는(EDLC·울트라커패시터) 순간정전·순간전압강하 보상장치를 2001년부터 한 분야만 해 온 국내 최초 개발사. UPS가 아니라 «Sag 전용» 직렬 보상장치(평시 상용전원 직결, 사고 시 2ms 절체). 단상 0.5kVA~삼상 2,400kVA 단일 라인업. 개별보상·중앙보상 두 방식을 한 라인업으로 제공.

## Operating Context

- 미팅 발표(전체화면, ←→·스와이프) → 링크 재열람 → 문의/사양서 요청 → 견적.
- 상단 바에서 언어·발표모드·이펙트 토글·WESCO 사이트 서랍(`sites.json`)·인사이트(블로그)·문의하기.
- 배포: GitHub Pages(`wesco-kr/wesco-intro`, CNAME intro.wesco.works). 단일 `index.html`(인라인 CSS/JS) + `i18n/*.json` + `present.js` + `launcher.js`.
- 검사 도구: `tools/check_i18n.py`(다국어 누락), `tools/render_book.py`(쪽별 PNG·넘침 검사).

## Capabilities and Constraints

- 기능은 유지한다(주인님 260924): 14쪽 판형, 발표모드, 5개국어, 이펙트 토글, 사이트 런처, 블로그 링크, Bing/GSC 인증 메타·파일(`google76444761ad356968.html` 삭제 금지).
- 수정 범위: 판형·톤 유지, 완성도 개선. 모바일은 축소 대신 세로 재배치까지 허용.
- 텍스트는 `data-i18n` 키로 `i18n/ko.json`(269키)에서 온다. 문구를 바꾸면 5개 언어 파일을 함께 봐야 한다(`tools/check_i18n.py`).
- 용어: TSP®는 UPS가 아니다. «배터리» 표현 금지(배터리리스/EDLC). 보상 표준 1초, 절체 2ms. 전기 파형은 사인파.
- 대외 수치 정본(260915 확정): 누적 50,000+대 · 900+ 고객사 · 15+ 국가 · 2001년 설립(25년). 「150,000+」「16개국」은 구버전 — 쓰지 않는다.
- 연혁에 **2026년 항목이 명확히 보여야** 최신본으로 인식된다(주인님 260924).
- 레퍼런스 고객 실명은 승인됨(CI 106종 `images/ci/`). BOM·원가·단가는 싣지 않는다.
- 미정: 표지에 «제품 + WESCO 마크 착용 엔지니어» 사진(제품 크기 체감용) — 실사 자산 확보 여부에 따름. 사람 사진은 AI 생성으로 대체하지 않는다(대외 신뢰).

## Brand Commitments

- 워드마크 WESCO + 심볼은 «소의 코뚜레»(Q 아님). `images/wesco_logo.png`.
- 팔레트 = 웜 스톤·아이보리 산업 전문서: 레드 `#C13816`(포인트), 헤딩 `#2C2924`, 본문 `#4A4641`, 배경 `#FDFCF9`/`#F7F4EE`, 보조 스틸블루 `#4F6886`. 검정 금지, 이모지 금지, 과장 장식 금지.
- 폰트: 한글 Noto Sans KR(맑은 고딕 폴백), 영문 Inter/Arial.
- 문서 톤: 간결·담백. 한글 단독이 기본(기술 고유명사만 영문).

## Evidence on Hand

- 제품 실사 `images/product/`, 설치 현장 `images/install/`·`images/proof/install_semi_2026.jpg`, HMI·Sag-VIEWER 화면 `images/hmi/`, 인증 `images/cert/`, 회사 `images/company/`, 고객사 CI 106종 `images/ci/`, 세계지도 `images/world-map.png`.
- 수치 근거: 사업소개서 Ver3.5 · 대리점 LIST V3.0(거점은 국가·도시만 표기).
- 없는 것: 제품과 사람이 함께 찍힌 표지용 사진(확인 필요) · 고객 추천사 · 제3자 벤치마크. 지어내지 않는다.

## Product Principles

1. 숫자와 용어는 정본에서만 — 대외 문서라 추정값·구버전 수치가 하나라도 있으면 신뢰가 무너진다.
2. 발표와 재열람을 같은 파일이 감당한다 — 큰 화면의 위계와 손안의 가독성을 둘 다 만족해야 한다.
3. 기능·판형은 지키고 완성도로 승부한다 — 위계·타이포·여백·일관성이 «AI가 만든 페이지» 인상을 걷어낸다.
4. 다국어는 한 몸 — 한 언어만 고치면 나머지 네 언어가 깨진다.
5. 지속 개선 — 1.0 이후 작은 단위로 계속 다듬는다(주인님 260924).
